import { Header as AppHeader } from 'react-native/Libraries/NewAppScreen';

/**
 * AppHeader component for native which uses react-native/Libraries/NewAppScreen
 * @component
 * @example
 * export { AppHeader }
 */
export { AppHeader };
